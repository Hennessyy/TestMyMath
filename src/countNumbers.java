import javax.swing.*;

public class countNumbers {
   /* public static void main(String[] args) {

        int [] array = new int[100];
        int [] checkArray = new int[100];
        for (int a = 0; a < array.length; a++) {
            array[a] = (a + 1);
        }

        int answer = 1;
        while( answer > 0 ){


             answer = Integer.parseInt(JOptionPane.showInputDialog(null,"Enter a number (0 to exit)"));
             if (answer > 0) {
                 checkArray[answer - 1]++;
             }
        }



        for (int x = 0 ; x < checkArray.length; x++){

            System.out.println(array[x]+"\t\t"+checkArray[x]);

        }
    }
*/

}
